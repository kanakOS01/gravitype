[project]
name = "gravitype"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "textual>=8.2.7",
]

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
    "ruff>=0.15.17",
    "textual-dev>=1.8.0",
]
